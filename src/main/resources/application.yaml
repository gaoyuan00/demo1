spring:
  data:
    redis:
      host: 127.0.0.1
      port: 6379
      database: 0
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/school?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: hgy7654321

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
security:
  jwt:
    # JWT签名密钥（用于签名与校验，防止篡改）
    secret: demo-jwt-secret-key-for-spring-security-2026
    # JWT有效时间（单位：毫秒，7200000=2小时）
    expiration-millis: 7200000